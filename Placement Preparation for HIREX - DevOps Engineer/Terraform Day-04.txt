====================================================================
      📁 Terraform Manifest File | Providers | Plugins
====================================================================

🔰 WHAT IS A MANIFEST FILE IN TERRAFORM?
--------------------------------------------------------------------
A **manifest file** is any `.tf` file written in HCL that defines
**infrastructure resources**, variables, outputs, and providers.

💡 Terraform doesn’t require a specific file name. All `.tf` files in a directory are read together.

📁 Example Manifest Files:
- `main.tf`         → Main configuration
- `variables.tf`    → Input variable definitions
- `outputs.tf`      → Output values
- `provider.tf`     → Provider configuration
- `terraform.tfvars` → Variable values

====================================================================
🧱 MANIFEST FILE STRUCTURE EXAMPLE
--------------------------------------------------------------------
```hcl
provider "aws" {
  region = "us-east-1"
}

resource "aws_instance" "demo" {
  ami           = "ami-0c55b159cbfafe1f0"
  instance_type = "t2.micro"
}

output "instance_id" {
  value = aws_instance.demo.id
}
````

Each block is part of your **Terraform manifest**.

\====================================================================
🌍 TERRAFORM PROVIDERS
----------------------

📘 A **provider** is a plugin that tells Terraform how to interact
with a specific **platform**, like AWS, Azure, GCP, Kubernetes, etc.

Terraform uses providers to **provision resources** on these platforms.

✅ Examples of Providers:

* `aws` → Amazon Web Services
* `azurerm` → Microsoft Azure
* `google` → Google Cloud Platform
* `kubernetes` → Kubernetes clusters
* `docker` → Manage Docker containers

---

📁 Basic AWS Provider Example:

```hcl
provider "aws" {
  region  = "us-east-1"
  profile = "default"
}
```

🌐 You can also configure credentials using:

* Environment variables
* AWS credentials file
* IAM roles (on EC2)

\====================================================================
🧩 WHAT ARE TERRAFORM PLUGINS?
------------------------------

A **plugin** in Terraform is the actual binary used to:
✔️ Connect to cloud providers
✔️ Authenticate
✔️ Create/read/update/delete resources

🧠 Think of it like this:
➡️ Terraform Core = Engine
➡️ Provider Plugin = Extension for AWS, Azure, etc.

---

📁 Where Are Plugins Stored?
After running:

```
terraform init
```

Terraform downloads the necessary provider plugins into:

```
.terraform/providers/
```

💡 No need to install manually — Terraform handles it.

\====================================================================
📦 CUSTOM OR THIRD-PARTY PROVIDERS
----------------------------------

You can even create your own **custom provider** or use community-contributed plugins from:

➡️ Terraform Registry: [https://registry.terraform.io/](https://registry.terraform.io/)

💡 Example:

```
provider "random" {
  # Generates random strings, numbers, etc.
}
```

\====================================================================
📘 SUMMARY
----------

✔️ Terraform Manifest File = `.tf` files that define your infrastructure
✔️ Providers = Interfaces to connect Terraform with external platforms
✔️ Plugins = Binaries that implement provider logic (downloaded via `terraform init`)
✔️ Everything is declarative and managed through code

💡 Learn manifest structure + providers → You’re ready for production IaC!

\====================================================================
